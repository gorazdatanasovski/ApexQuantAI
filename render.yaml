services:
  - type: web
    name: apexquantai
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install --upgrade pip && pip install . && pip install fastapi uvicorn
    startCommand: uvicorn src.quantai.interface.server:app --host 0.0.0.0 --port $PORT