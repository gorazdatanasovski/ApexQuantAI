services:
  - type: web
    name: apexquantai
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install --upgrade pip && pip install fastapi uvicorn pydantic
    startCommand: uvicorn --app-dir src quantai.interface.server:app --host 0.0.0.0 --port $PORT
